#include <iostream>
using namespace std;

int main() {

char letra;
bool found = false;

while(cin>>letra and not found and letra != '.'){
    

    if(letra == 'a') 
    found=true;
}
 if(found) {cout<<"yes";}
 else {cout<<"no";}
 cout<<endl;
}